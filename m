//10727207 張婷_10727240 賴卷狄
#include <iostream>
#include <cstdio>
#include <string>
#include <string.h>
#include <vector> 
#include <stdlib.h> 
#include <stdio.h> 
/* run this program using the console pauser or add your own getch, system("pause") or input loop */

using namespace std;
 
void skipWhiteSpace()// 得到一個非空白的字元
bool isDigit()//看是否數字
bool isoperator // 看是否為運算子
bool checkbrackets // 看括號有沒有對齊
bool checkPreviousChar()//去看上一個有無任何Error







int main(int argc, char** argv) {

	return 0;
}
